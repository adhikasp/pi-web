---
"@jmfederico/pi-web": patch
---

When the app is opened directly into a specific project and conversation (deep link), keep only the Sessions section of the left sidebar expanded by default. Projects, Workspaces, and Scheduled Tasks start collapsed so the chat has the focus. Other entry points still start with every section expanded.
